<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>FECS</RootNamespace>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
  </PropertyGroup>

  <!-- If tests need to access internal types, keep this; otherwise delete. -->
  <ItemGroup>
    <InternalsVisibleTo Include="FECS.Tests" />
  </ItemGroup>
</Project>

